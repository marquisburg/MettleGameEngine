// Mettle standard library: System
//
// Process spawning via C runtime system().
// Use for running shell commands (Mettle, nasm, gcc, git, curl, etc.).
//
// On Windows: invokes cmd.exe /c <cmd>
// On Linux: invokes sh -c <cmd>
//
// Returns the command's exit status, or -1 on error.

export extern function system(cmd: cstring) -> int32 = "system";
